#include <iostream>

using namespace std;

struct student {
	char name[50], surname[50], group[10];
};

student drop(student g, char n) {
	
}

int main(){
	int choose;
	cout << "выберите задание" << '\n';
	cout << "1" << '\n';
	cout << "2" << '\n';
	cin >> choose;
	if (choose == 1) {
		int size = 10;
		int j = 10;
		const char* words[] = { "C++", "is", "one", "of", "the", "world's", "most", "popular", "programming", "languages" };
		for (int i = 0; i < (size / 2); i++)
		{
			const char* f = words[j - 1];
			words[j - 1] = words[i];
			words[i] = f;
			j--;
		}
		for (int i = 0; i < size; i++)
		{
			cout << words[i] << '\t';
		}
	}
	if (choose == 2) {
		student g = {"Гена", "Самойленко", "ТИ-23"};
		char n[50];
		cout << "Введите параметры: ";
		cin >> n;
		student* ptr = &g;
	}
}
